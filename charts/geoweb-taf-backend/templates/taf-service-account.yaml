{{- $dbUsesAws := and (eq .Values.taf.db.mode "external") (eq .Values.taf.db.external.source "secretProvider") (eq .Values.taf.db.external.secretProvider.provider "aws") }}
{{- if $dbUsesAws }}
{{- if not .Values.taf.secretServiceAccount }}
{{- fail "taf.secretServiceAccount is required when an AWS secret provider is used" }}
{{- end }}
{{- if not .Values.taf.db.external.secretProvider.iamRoleARN }}
{{- fail "taf.db.external.secretProvider.iamRoleARN is required when an AWS secret provider is used" }}
{{- end }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.taf.secretServiceAccount }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.taf.db.external.secretProvider.iamRoleARN }}
{{- end }}
