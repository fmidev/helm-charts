apiVersion: v1
kind: Service
metadata:
  name: {{ include "harp.fullname" . }}
  labels:
    {{- include "harp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "harp.selectorLabels" . | nindent 4 }}
