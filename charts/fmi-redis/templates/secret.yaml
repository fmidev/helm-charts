# Automatically generated secret if auth is enabled, but no secret is provided.
{{- if and .Values.auth.enabled (not .Values.auth.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: {{ .Release.Name }}
type: Opaque
stringData:
  redis-password: {{ required "auth.password is required when auth.enabled=true and auth.existingSecret is unset" .Values.auth.password | quote }}
{{- end }}
