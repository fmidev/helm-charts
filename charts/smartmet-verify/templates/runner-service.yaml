{{- if .Values.runner.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartmet-verify.componentFullname" (list . "runner") }}
  labels:
    {{- include "smartmet-verify.componentLabels" (list . "runner") | nindent 4 }}
spec:
  type: {{ .Values.runner.service.type }}
  selector:
    {{- include "smartmet-verify.componentSelectorLabels" (list . "runner") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.runner.service.port }}
      targetPort: http
      protocol: TCP
{{- end }}
