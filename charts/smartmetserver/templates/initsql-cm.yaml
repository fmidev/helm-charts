apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Values.smartmetserver.name }}-{{ .Values.initSqlCm.name }}
  labels:
    app: {{ .Values.smartmetserver.name }}
  namespace: {{ .Release.Namespace }}
data:
  init.sql: |
    {{- .Values.initSqlCm.initSql | nindent 4 }}
